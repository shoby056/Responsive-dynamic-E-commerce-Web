import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['fakestoreapi.com'], // Add external domain for images
  },
  // Other config options can go here
};

export default nextConfig;
